# Use TypeScript And Node For The First Implementation

The first implementation will use TypeScript and Node across platform packages, server assembly, the embeddable widget, config validation, and the custom tool SDK. This keeps the first product boundary type-safe across frontend, backend, and customer-authored tools. Other runtimes, especially Python for data-heavy tools or isolated agent workers, can be added later behind tool execution or agent runtime interfaces.

