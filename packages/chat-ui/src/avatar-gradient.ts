export function avatarGradient(_seed: string): string {
  return "linear-gradient(135deg, color-mix(in srgb, var(--primary) 82%, white), color-mix(in srgb, var(--primary) 78%, var(--accent-foreground)))";
}
